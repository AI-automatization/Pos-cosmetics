[build]
dockerfilePath = "Dockerfile"

[deploy]
# Worker — HTTP port yo'q, shuning uchun healthcheck PATH yo'q
startCommand = "node dist/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
