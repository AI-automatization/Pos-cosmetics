# Root railway.toml — placeholder only.
# Har bir service o'z papkasidagi railway.toml ni ishlatadi:
#   web    → apps/web/railway.toml    (Root Directory: apps/web)
#   api    → apps/api/railway.toml    (Root Directory: apps/api)
#   bot    → apps/bot/railway.toml    (Root Directory: apps/bot)
#   worker → apps/worker/railway.toml (Root Directory: apps/worker)
